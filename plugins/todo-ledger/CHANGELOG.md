# Changelog

## 0.1.0 — 2026-07-07
- Initial release: `ledger` skill — inventories TODO/FIXME/HACK/XXX comments via
  `git grep`, dates and attributes each with `git blame`, ranks oldest-first with
  a worst-offenders summary and copyable file:line references. Read-only by
  default; the dated report file is written only on request.
