# Changelog — test-gap-nudge

## 0.1.0 — Unreleased
- Initial release: advisory `Stop` hook. Source-vs-test classification of
  `git status --porcelain`, one `systemMessage` nudge per session, fail-open
  everywhere (always exit 0), read-only except a TMPDIR marker.
